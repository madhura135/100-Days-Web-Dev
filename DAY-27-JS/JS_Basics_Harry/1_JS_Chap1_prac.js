// Create a variable of type string and try to add number to it

let str = "may";
console.log(
  "Type of string is: " + typeof str,
  "& string is: " + str,
  "Adding number to it means concatinate: " + str + 9,
);
