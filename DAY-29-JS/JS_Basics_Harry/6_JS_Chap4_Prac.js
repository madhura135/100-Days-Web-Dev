// The Ternary: You have const isLogged = true;. Use interpolation to print Status: Online if true,
// and Status: Offline if false, using a ternary operator inside the ${}.
const isLogged = true;
console.log(`${isLogged == true ? "Status:Online" : "Status:Offline"}`);
